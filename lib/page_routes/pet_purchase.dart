import 'package:flutter/material.dart';

class PetPurchase extends StatelessWidget {
  const PetPurchase({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}